from setuptools import setup, find_packages

setup(
    name='permutandis',
    version='0.0.1',
    description='Permutandis for tabletop prehensile rearrangement planning',
    packages=find_packages()
)
